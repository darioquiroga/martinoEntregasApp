import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.martinoentregas',
  appName: 'martinoEntregas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
