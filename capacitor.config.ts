import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app.martino.entregas',
  appName: 'martinoEntregas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
